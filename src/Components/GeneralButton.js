const GeneralButton = ({ text }) => {
    return(
        <button className="w-screen h-12 bg-purple-800 rounded-t-3xl text-white font-bold">
            {text}
        </button>
    )
}

export default GeneralButton